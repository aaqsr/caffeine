import * as React from 'react';
import { FC } from 'react';
import { Fade } from 'react-reveal'

export const Header: FC = () => (
    <h1>
        <Fade bottom cascade>
            CAFFEINE
        </Fade>
    </h1>
);